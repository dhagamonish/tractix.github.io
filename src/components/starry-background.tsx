
'use client';

export function StarryBackground() {
  // This component is no longer used for the main background, 
  // but kept in case it's needed elsewhere.
  // The VantaBackground component now provides the animated background.
  return null;
}
